apiVersion: v1
kind: Service
metadata:
  name: sre-copilot-svc
  namespace: default
spec:
  selector:
    app: sre-copilot
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
