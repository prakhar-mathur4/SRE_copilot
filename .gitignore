# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.env

# Node
node_modules/
dist/
.env.local

# OS
.DS_Store
 
# Folder


# Local infrastructure config — contains environment-specific URLs and IPs.
# Copy connectors.example.json to connectors.json and fill in your values.
connectors.json

# SSL monitor domain list — contains internal domain names.
# Copy ssl_domains.example.json to ssl_domains.json and fill in your domains.
ssl_domains.json

# Runtime logs and PID files
*.log
.backend.pid
.frontend.pid

# Added by code-review-graph
.code-review-graph/

# Internal design docs — not for repo
design.md
to-do.md

# Auth store (SQLite) — never commit local credentials DB
auth.db
auth.db-shm
auth.db-wal
